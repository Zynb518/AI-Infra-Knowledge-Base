# 仓库协作约束

## 子知识库目录规范

本规范适用于 `Knowledge/` 下所有当前及未来的子知识库。

### 课程式目录模型

每个子知识库都视为一门独立课程，并严格采用“章节 → 主题 → Markdown 笔记”的内容层级：

```text
<子知识库>/
├── 知识地图.md
├── 知识地图.mmd
├── <章节一>/
│   ├── <主题一>/
│   │   ├── <具体笔记一>.md
│   │   └── <具体笔记二>.md
│   └── <主题二>/
│       └── <具体笔记>.md
├── <章节二>/
│   └── <主题>/
│       └── <具体笔记>.md
└── <章节三>/
    └── <主题>/
        └── <具体笔记>.md
```

层级含义：

1. 子知识库是课程根目录。
2. 子知识库下的第一层内容目录是章节。
3. 章节下的第一层目录是主题。
4. 具体的 Markdown 笔记直接放在主题目录中。
5. 主题目录下不得继续建立内容子目录。

### 章节与主题设计

- 章节的名称和数量不固定，由 Agent 根据子知识库的知识范围、学习顺序和内在结构决定。
- 主题的名称和数量不固定，由 Agent 根据章节内容决定。
- 章节之间应尽量边界清晰，并共同覆盖该子知识库的核心知识范围。
- 主题应是章节内部可独立学习和整理的知识单元。
- 不要为了追求目录数量统一而拆分或合并章节、主题。
- 主题目录和笔记文件应在实际开始学习或写作时再创建，不要根据知识地图提前批量建立空目录或空笔记。

### 文件放置规则

- 不得在子知识库根目录或章节目录中直接放置具体知识笔记。
- 具体知识笔记必须放在对应的主题目录中。
- 同一主题可以包含一篇或多篇 Markdown 笔记。
- 主题目录下不得创建更深层级的内容目录。
- `知识地图.md` 是子知识库入口，可以保留在子知识库根目录。
- `知识地图.mmd` 是可选的 Mermaid 导出源文件，可以保留在子知识库根目录。
- `.obsidian/` 等编辑器配置目录不计入课程内容层级。

### 知识地图规则

- 知识地图负责定义课程的知识范围、章节、主题、推荐学习顺序和完成标准。
- 知识地图的预规划粒度必须在主题层级停止，只能事先规划章节和主题。
- 知识地图不得为主题继续细分具体笔记，不得预先列出 Markdown 文件节点、文件数量、文件名或拆分粒度。
- Markdown 中的全景知识目录使用 `text` 代码块表示目录树。
- 如需生成图片，将对应 Mermaid `tidy-tree` 源码保存为子知识库根目录下的 `知识地图.mmd`。
- 文本目录树和 Mermaid 源码必须保持相同的“子知识库 → 章节 → 主题”结构，并同时在主题层级停止。

### Markdown 规则

- 所有笔记使用标准 Markdown 语法。
- 链接使用 `[标题](相对路径)` 语法。
- 禁止使用 Obsidian 专属的双向链接语法。

## 课程笔记讲课风格

- 每篇具体笔记视为一课，开头必须有“引入”，说明主题背景、实际用途或要解决的问题。
- 每课只聚焦一个知识主题，内容应紧凑、实用、易懂，避免无关扩展和一次引入过多新概念。
- 讲解应先使用直白语言建立认识，再引入必要术语；命令、代码和示例必须说明用途以及需要观察的现象。
- 每课必须附带一个或多个简单、可执行且有实际意义的课后小实操，优先使用本地已有环境，避免不必要的复杂配置、权限操作和破坏性命令。
- 实操数量应根据本课内容复杂度灵活设置：内容简单且较少时合并为一个实操，内容较多时最多设置两个实操，不得为了凑数量拆分练习。
- 课后小实操不能与课堂正文或示例完全相同，必须在已讲内容的基础上改变输入、参数、文件结构、命令组合或任务目标，促使学生举一反三。
- 课后小实操只能使用本课已经讲解的基础知识，不得为了增加变化而引入尚未讲解的命令、语法、选项或概念；如确实需要新知识，必须先在正文中讲清楚。
- 课后小实操应尽量让学生手动输入关键命令和脚本内容，避免把完整答案设计成可以直接整段复制执行的步骤；必要的固定内容可以提供，但应拆成清晰的小步并说明需要观察的现象。
- 后续课后小实操采用“题目模式”：只描述任务目标、输入条件、限制和必要提示，不提供能够直接完成任务的完整命令序列、脚本代码或参考答案；课堂正文和示例可以讲解答案，但实操必须由学生独立完成。
- 学生完成当前课程的实操并提交实际运行结果后，才能进入下一课；Agent 应先根据提交结果帮助检查和纠正问题，不应在学生未完成当前实操时主动开始下一课。
- 对于代码类课程或脚本类实操，内容顺序必须是先创建并完成代码，再执行代码，最后记录运行结果；不得在代码之前要求或展示运行结果。
- Agent 为验证课程命令而创建的临时文件、目录或输出，验证完成后必须按明确路径清理；清理前要确认目标是 Agent 自己创建的临时内容，不得误删学生或用户已有的练习文件。
- 不单独设置“本课目标”“思考”和“概括”栏目。每个实操使用三级标题“实操一”“实操二”等；其下不再设置更低级标题。代码类实操依次包含“操作步骤”“代码”和“运行结果”三个部分，非代码类实操依次包含“操作步骤”和“运行结果”两个部分。
- 实操后不要求学生另写概括，因为相关概念、操作含义和结果解释应当已经在正文与“小结（掌握范围）”中讲清楚，避免重复作答。
- 小结必须命名为“小结（掌握范围）”，只总结本课结束后学生必须掌握的知识和能力，不加入拓展内容或尚未讲解的内容。
- 推荐采用以下紧凑结构：

````text
# 课程标题

## 引入

## 核心内容

## 示例

## 小结（掌握范围）

## 课后小实操

### 实操一

实操说明和目的。

**操作步骤**

```bash
# 学生复制运行
```

**代码**

```bash

```

**运行结果**

```text


```

### 实操二

按同样结构编写；如不需要多个实操，可以省略。
````

- 每个实操必须按规定顺序包含对应部分，使用加粗标签而非标题。代码类实操的“代码”部分必须留出空的代码块，并根据实际代码类型灵活选择语言标记，例如 Bash 使用 bash、Python 使用 python；“运行结果”部分统一留出空的 text 代码块，供学生粘贴实际终端输出。

## 综合实战与项目安排

- 综合实战不是每篇课程笔记的固定组成部分，应在一个主题或阶段的核心知识已经讲完，并且学生完成相关基础实操后再安排。
- 进入综合实战前，Agent 应确认学生已经能够独立使用该阶段的主要知识；项目需求不得依赖尚未讲解的核心概念、方法、工具或技术。
- 如果项目确实需要新知识，应先把新知识安排成独立课程，讲解并练习完成后，再把它纳入综合实战。
- 综合实战开始时，Agent 应在对话或终端中展示由简到难的可选项目，等待学生明确选择；选项至少分为“简单”“中等”“困难”三个等级，不直接替学生指定项目。
- 简单项目应组合少量已学知识，目标明确、输入和产出简单，适合验证基础能力。
- 中等项目应组合多个已学主题，形成完整的多步骤处理流程，并要求学生处理基本输入、结果验证或异常情况。
- 困难项目应接近一个可重复使用的小工具或小功能，要求更完整的结构组织、边界处理、结果交付和维护说明，但仍不得超出已学知识范围。
- 每个可选项目至少说明项目背景、任务目标、输入输出、限制条件、验收标准和建议使用的已学知识，让学生能够根据难度和兴趣选择。
- 仓库根目录 Projects/ 只是项目容器，不是具体项目根目录；不得在其中创建 Projects.md 或其他项目清单文件。
- 学生必须先从 Agent 在对话或终端展示的选项中明确选择一个项目，Agent 才能创建具体项目目录；不得在学生选择前提前创建具体项目目录。
- 具体项目目录必须按三位数字顺序加项目名称命名，例如 Projects/001-日志分析报告器/、Projects/002-路径分类统计器/。
- 新项目编号应基于 Projects/ 中已有的项目目录递增，不得复用已有编号；项目目录名称应保持简洁、稳定且能表达项目名称。
- 每个具体项目目录必须包含 README.md，README.md 至少说明项目背景、使用到的知识、知识树、项目要求、输入输出、限制条件和验收标准；项目代码、数据、报告和其他相关文件都放在对应项目目录中。
- 项目 README.md 中的知识树使用 text 代码块，并按照“子知识库 → 章节 → 主题”绘制，使用 ├──、└── 和缩进表示层级，知识范围停止在主题层级，例如：

~~~text
Linux 与操作系统
└── 基础使用
    ├── 常用命令与文本处理
    └── Bash 脚本
~~~

- Projects/ 根目录不得直接放置具体项目的代码、数据或报告；.gitkeep 等版本控制占位文件不受此限制。
- Projects/ 不属于任何一个子知识库的章节或主题，知识地图不应增加 Projects 节点。
- 项目实操同样采用题目模式，不提供完整实现答案；代码类项目必须依次包含“操作步骤”“代码”和“运行结果”。
- 学生完成所选项目并提交代码和实际运行结果后，Agent 应先检查验收标准，再决定是否进入下一个难度等级或新的主题。
